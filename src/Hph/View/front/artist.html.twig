{% extends "base.html.twig" %}
{% block title %}Artiste{% endblock %}
{% block titleMobile %}ARTISTE{% endblock %}
{% block content %}
    <div class="container hidden-xs">
        <div class="row">
            <div class="col-xs-8">
                <div class="content-div">
                    <h1 class="font-circula upper">{{ artist.name }}</h1>
                    <p>{{ artist.bio }}</p>
                    <div class="artiste-tag upper">
                        {% for tag in tags %}
                            {% if loop.index%2 == 0 and loop.index<tags|length %}
                                <div class="font-circula tag_artist"> {{ tag.name }} <span class="orange">/</span></div>
                            {% elseif loop.index%2 != 0 and loop.index<tags|length %}
                                <div class="font-circula tag_artist"> {{ tag.name }} <span class="turquoise">/</span></div>
                            {% elseif loop.index%2 != 0 %}
                                <div class="font-circula tag_artist"> {{ tag.name }}</div>
                            {% else %}
                                <div class="font-circula tag_artist"> {{ tag.name }}</div>
                            {% endif %}
                        {% endfor %}
                    </div>
                    <br />
                    <hr class="hr">
                </div>
                <br />
                <iframe width="100%" height="480" src="{{ artist.videoUrl }}" frameborder="0" allowfullscreen></iframe>
            </div>
            <div class="col-xs-4">
                <div class="content-div upper text-center">
                    <img src="../../../web/img/artist/{{ artist.imgArtist }}" class="img-255">
                    <hr class="hr">
                    {% for prog in progs %}
                        {% if prog.showcase == 0  %}
                            <div class="text-center">
                                <h2 class="font-circula">{{prog.concert_start |  localizeddate('long', 'none', 'fr')}}</h2>
                                <hr class="hr-small">
                                <h3 class="font-circula prog_place"><a class="white" href="{{ prog.url }}" >{{ prog.name }}</a> </h3>
                                <h4 class="font-circula prog_hour">{{ prog.concert_start  | localizeddate('none', 'short', 'fr') }} <span class="turquoise bold">|</span> {{ prog.concert_end | localizeddate('none', 'short', 'fr') }}</h4>
                            </div>

                        {% endif %}
                        {% if prog.showcase == 1  %}
                            <div class="showcase">
                                <h2 class="font-circula">{{prog.concert_start |  localizeddate('long', 'none', 'fr')}}</h2>
                                <hr class="hr-small">
                                <h5 class="font-circula white text-center showcase">SHOWCASE :</h5>
                                <h5 class="white text-center showcase">
                                    {{ prog.name }}
                                </h5>
                                <h6 class="font-circula white text-center prog_hour">
                                    {{ prog.concert_start | localizeddate('none', 'short', 'fr') }}
                                    <span class="turquoise bold">|</span>
                                    {{ prog.concert_end | localizeddate('none', 'short', 'fr') }}
                                </h6>
                            </div>
                        {% endif %}
                        <hr class="hr">
                    {% endfor %}
                </div>
                <div class="artiste-social text-center">
                    <a href="{{ artist.facebookUrl }}"><i class="fa fa-facebook-square social-logo" aria-hidden="true"></i></a>
                    <a href="{{ artist.twitterUrl }}"><i class="fa fa-twitter-square social-logo" aria-hidden="true"></i></a>
                    <a href="#"><i class="fa fa-spotify social-logo" aria-hidden="true"></i></a>
                    <a href="{{ artist.youtubeUrl }}"><i class="fa fa-youtube-square social-logo" aria-hidden="true"></i></a>
                </div>
            </div>
        </div>
    </div>
    <div class="visible-xs container">
        <div class="col-xs-12">
            <div class="content-div text-center">
                <img src="../../../web/img/artist/{{ artist.imgArtist }}" class="img-255">
                <h1 class="font-circula">{{ artist.name }}</h1>
                <p>{{ artist.bio }}</p>
                <div class="artiste-tag">
                    {% for tag in tags %}
                        {% if loop.index%2 == 0 and loop.index<tags|length %}
                            <div class="font-circula tag_artist"> {{ tag.name }} <span class="orange">/</span></div>
                        {% elseif loop.index%2 != 0 and loop.index<tags|length %}
                            <div class="font-circula tag_artist"> {{ tag.name }} <span class="turquoise">/</span></div>
                        {% elseif loop.index%2 != 0 %}
                            <div class="font-circula tag_artist"> {{ tag.name }}</div>
                        {% else %}
                            <div class="font-circula tag_artist"> {{ tag.name }}</div>
                        {% endif %}
                    {% endfor %}
                </div>
                <hr class="hr">
                {% for prog in progs %}
                    <div class="text-center">
                        <h2 class="font-circula">{{ prog.concert_start | localizeddate('long', 'none', 'fr') }}</h2>
                        <h3 class="font-circula prog_place"><a class="white" href="{{ prog.url }}" >{{ prog.name }}</a></h3>
                        <h4 class="font-circula prog_hour">{{ prog.concert_start | localizeddate('none', 'short', 'fr') }} <span class="turquoise bold">|</span> {{ prog.concert_end | localizeddate('none', 'short', 'fr') }}</h4>
                        {% if not prog.showcase ==0  %}
                            <hr class="hr-small">
                            <div class="showcase">
                                <h5 class="font-circula white text-center showcase">
                                    SHOWCASE :
                                </h5>
                                <h5 class="white text-center showcase">
                                    {{ prog.name }}
                                </h5>
                                <h6 class="font-circula white text-center prog_hour">
                                    {{ prog.concert_start | localizeddate('none', 'short', 'fr') }}
                                    <span class="turquoise bold">|</span>
                                    {{ prog.concert_end | localizeddate('none', 'short', 'fr') }}
                                </h6>
                            </div>
                        {% endif %}
                        <hr class="hr">
                        <div class="artiste-social text-center">
                            <a href="{{ artist.facebookUrl }}"><i class="fa fa-facebook-square social-logo" aria-hidden="true"></i></a>
                            <a href="{{ artist.twitterUrl }}"><i class="fa fa-twitter-square social-logo" aria-hidden="true"></i></a>
                            <a href="#"><i class="fa fa-spotify social-logo" aria-hidden="true"></i></a>
                            <a href="{{ artist.youtubeUrl }}"><i class="fa fa-youtube-square social-logo" aria-hidden="true"></i></a>
                        </div>
                        <br />
                        <iframe width="100%" height="200" src="{{ artist.videoUrl }}" frameborder="0" allowfullscreen></iframe>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}