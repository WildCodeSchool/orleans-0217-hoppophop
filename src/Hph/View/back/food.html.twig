{% extends "base.html.twig" %}
{% block title %}Foodtruck{% endblock %}
{% block content %}
    <div class="container">
        <h2 class="text-center">Ajouter un Foodtruck</h2>
        <hr/>
        <form method="POST" action="../../../../admin/admin.php?page=addFood" enctype="multipart/form-data">
            <div class="form-group">
                <label for="title">Nom du Foodtruck</label>
                <input type="text" class="form-control" id="name" name="name" placeholder="Entrez le nom du Foodtruck">
            </div>
            <div class="form-group">
                <label for="text">Description du Foodtruck</label>
                <textarea class="form-control textarea" name="content" id="content" placeholder="Entrez la description du Foodtruck" rows="10"></textarea>
            </div>
            <div class="form-group">
                <label for="start">Heure d'ouverture du Foodtruck : 2017-09-15 17:41:58</label>
                <input type="datetime" class="form-control" placeholder="Format : YYYY-MM-DD HH:MM:SS" id="start" name="start">
            </div>
            <div class="form-group">
                <label for="start">Heure de fermeture du Foodtruck : 2017-09-17 08:01:06</label>
                <input type="datetime" class="form-control" placeholder="Format : YYYY-MM-DD HH:MM:SS" id="end" name="end">
            </div>
            <div class="form-group">
                <label for="place">Lieu du Foodtruck</label>
                <select multiple class="form-control" id="place" name="place">
                    {%  for place in places %}
                        <option value="{{ place.id }}">{{ place.name }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="form-group">
                <label for="img">Photo du Foodtruck</label>
                <input type="file" id="img" name="img">
                <p class="help-block">Aide à la photo</p>
            </div>
            <div class="text-center">
                <input type="submit" name="submit" class="btn btn-primary" value="Créer">
            </div>
        </form>
        <br />
        <h2 class="text-center">Liste des Foodtruck</h2>
        <hr />
        <table class="table table-hover">
            <thead>
            <tr>
                <th>#</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Image</th>
                <th>Lieux</th>
                <th>Début</th>
                <th>Fin</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {%  for food in foods %}
                <tr>
                    <th scope="row">{{ food.id }}</th>
                    <td>{{ food.name }}</td>
                    <td>{{ food.content }}</td>
                    <td><img src="../../../../web/img/foodtruck/{{ food.imgEat }}" alt="Image du Foodtruck" class="img-list"></td>
                    <td>{{ food.place_name }}</td>
                    <td>{{ food.start|date('l') }}</td>
                    <td>{{ food.end|date('l') }}</td>
                    <td>
                        <button type="button" class="btn btn-success" data-toggle="modal" data-target="#edit{{ food.id }}"><i class="fa fa-pencil"></i></button>
                        <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#delete{{ food.id }}"><i class="fa fa-trash-o"></i></button>
                    </td>
                </tr>
                <div class="modal fade" id="edit{{ food.id }}" tabindex="-1" role="dialog">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                <h4 class="modal-title">Modifier un Foodtruck</h4>
                            </div>
                            <form method="POST" action="../../../../admin/admin.php?page=updateFood" enctype="multipart/form-data">
                                <div class="modal-body">
                                    <div class="form-group">
                                        <label for="title">Nom du Foodtruck</label>
                                        <input type="text" class="form-control" id="name" name="name" placeholder="Entrez le nom du Foodtruck" value="{{ food.name }}">
                                    </div>
                                    <div class="form-group">
                                        <label for="content">Description du Foodtruck</label>
                                        <textarea class="form-control textarea" name="content" id="content" placeholder="Entrez la description du Foodtruck" rows="10">{{ food.content }}</textarea>
                                    </div>
                                    <div class="form-group">
                                        <label for="start">Heure d'ouverture du Foodtruck : 2017-09-15 17:41:58</label>
                                        <input type="datetime" class="form-control" placeholder="Format : YYYY-MM-DD HH:MM:SS" id="start" name="start" value="{{ food.start }}">
                                    </div>
                                    <div class="form-group">
                                        <label for="start">Heure de fermeture du Foodtruck : 2017-09-17 08:01:06</label>
                                        <input type="datetime" class="form-control" placeholder="Format : YYYY-MM-DD HH:MM:SS" id="end" name="end" value="{{ food.end }}">
                                    </div>
                                    <div class="form-group">
                                        <label for="place">Lieu du Foodtruck</label>
                                        <select multiple class="form-control" id="place" name="place">
                                            {%  for place in places %}
                                                <option value="{{ place.id }}">{{ place.name }}</option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                    <div class="form-group">
                                        <label for="img">Photo du Foodtruck</label>
                                        <input type="file" id="img" name="img">
                                        <p class="help-block">Aide à la photo</p>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <input type="hidden" name="id" value="{{ food.id }}">
                                    <input type="submit" name="submit" class="btn btn-primary" value="Modifier">
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                <div class="modal fade" id="delete{{ food.id }}" tabindex="-1" role="dialog">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                <h4 class="modal-title" id="myModalLabel">Supprimer un Foodtruck</h4>
                            </div>
                            <div class="modal-body">
                                Êtes-vous sûr de vouloir supprimer le Foodtruck {{ food.name }} ?
                            </div>
                            <div class="modal-footer">
                                <form method="POST" action="../../../../admin/admin.php?page=deleteFood">
                                    <input type="hidden" name="id" value="{{ food.id }}">
                                    <input type="submit" name="submit" class="btn btn-danger" value="Oui">
                                </form>
                                <button class="btn btn-default" data-dismiss="modal">Non</button>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}